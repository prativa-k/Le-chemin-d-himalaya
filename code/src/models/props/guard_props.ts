type GuardProps = {
    roles: string[];
    // children permet de définir que le composant va posséder des élements enfants
    children: React.JSX.Element | React.JSX.Element[]
};

export type {GuardProps};