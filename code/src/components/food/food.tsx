import styles from "../../assets/css/food.module.css";

const Food = () => {
    return (
        <main>
            <div className={styles.titlebox}>
                <h2 className={styles.title}>Notre Creation</h2>
            </div>
        </main>
    );
};

export default Food;